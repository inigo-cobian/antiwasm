#include "../includes/wasm_utils.hpp"

char* bytesToHexChar(uint8_t* signedLittleEndianBytes, int nBytes)
{
    auto* formattedHexBytes = new char(nBytes * 2);
    for(int i = 0; i < nBytes; i++) {

    }
    return nullptr;
}